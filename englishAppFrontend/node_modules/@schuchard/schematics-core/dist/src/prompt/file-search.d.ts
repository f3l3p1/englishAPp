import { SchematicContext } from '@angular-devkit/schematics';
export interface IQueryFileSystem {
    isDir: boolean;
    dir: string;
    isSpec: boolean;
    path: string;
}
export declare function queryFileSystem({ context, defaultSearch, rootPath, }: {
    context: SchematicContext;
    defaultSearch?: string;
    rootPath?: string;
}): Promise<IQueryFileSystem>;
