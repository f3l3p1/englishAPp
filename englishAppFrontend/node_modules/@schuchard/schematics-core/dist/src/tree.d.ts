import { Tree, Rule, SchematicContext } from '@angular-devkit/schematics';
import * as ts from 'typescript';
/**
 * Don't add file at `path` if it already exists.
 */
export declare function filterExistingPath(tree: Tree, filePath: string, context?: SchematicContext): Rule;
/**
 * Return the matching nodes based on the predicate or all the nodes.
 */
export declare function findSourceNodes(sourceFile: ts.SourceFile, filter: (node: ts.Node) => boolean, debug?: boolean): ts.Node[];
/**
 * Update file, creating it if doesn't exist
 */
export declare function createOrUpdate(host: Tree, path: string, content: string): void;
/**
 * Get a list of all file paths
 */
export declare function getAllFilePaths({ nodePath, excludePath, }: {
    nodePath?: string;
    excludePath?: (nodePath: string) => boolean;
}): string[];
